# traceAI-livekit

OpenTelemetry instrumentation for LiveKit applications with Future AGI integration.

## Installation

```bash
pip install traceai-livekit
```

## Usage

```python
from traceai_livekit import enable_livekit_tracing

# Enable tracing with default settings (HTTP transport)
enable_livekit_tracing()
```

